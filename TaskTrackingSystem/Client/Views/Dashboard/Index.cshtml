
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<script data-main="assets/ts/tasks/task.entry.js" src="../Scripts/require.js"></script>

<div ng-controller="task.controller">
    @*<table class="table table-striped table-bordered nowrap" width="100%">
        <thead>
        <tr>
            <th>Number</th>
            <th>Specification</th>
            <th>CreationDate</th>
            <th>FinishDate</th>
            <th>Priority</th>
            <th>Status</th>
        </tr>
        </thead>
        <tbody>
        <tr ng-repeat="task in tasks">
            <td>{{ task.Number }}</td>
            <td>{{ task.Specification }}</td>
            <td>{{ task.CreationDate | date:"MM/dd/yyyy h:mma"}}</td>
            <td>{{ task.FinishDate| date:"MM/dd/yyyy h:mma" }}</td>
            <td>{{ task.Priority }}</td>
            <td>{{ task.Status }}</td>
        </tr>
        </tbody>
    </table>
    
    
    <table ng-table class="table" show-filter="true">
        <tr ng-repeat="task in tasks">
            <td title="'Specification'" filter="{ Specification: 'text'}" sortable="'Specification'">
                {{user.Specification}}
            </td>
            <td title="'Number'" filter="{ Number: 'number'}" sortable="'Number'">
                {{user.Number}}
            </td>
        </tr>
    </table>*@
    
    
    <div>
        <table ng-table class="table">
            <tr ng-repeat="user in users | orderBy:sortType:sortReverse">
                <td>{{user.name}}</td>
                <td>{{user.age}}</td>
            </tr>
        </table>
    </div>
</div>